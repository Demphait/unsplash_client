name: data

environment:
  sdk: ">=2.19.3 <3.0.0"

dependencies:
  domain:
    path: ../domain
  core:
    path: ../core

  # networking
  dio: 5.1.1
  retrofit: 4.0.1

  #json
  json_annotation: 4.8.0

  # database

  # di
  get_it:
  injectable:

  # utils
  equatable:


dev_dependencies:
  build_runner:
  injectable_generator:
  json_serializable: 6.6.1
  retrofit_generator: 6.0.0+1
